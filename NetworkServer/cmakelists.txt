#--------------------------------------------------------------------------
# gscept laboration environment
#--------------------------------------------------------------------------
PROJECT(NetworkServer)
CMAKE_MINIMUM_REQUIRED(VERSION 3.2)

FILE(GLOB NetworkServer_header src/*.h)
FILE(GLOB NetworkServer_sources src/*.cc)


SET(files_NetworkServer ${NetworkServer_headers} ${NetworkServer_sources})
SOURCE_GROUP("NetworkServer" FILES ${files_NetworkServer})

ADD_EXECUTABLE(NetworkServer ${files_NetworkServer})

IF(CMAKE_COMPILER_IS_GNUCXX)
    SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11")
ENDIF()

# IF(MSVC)
#     SET()
# ELSE()
#     SET(pthread)
# ENDIF()

SET(GSCEPT_LAB_ENV_ROOT ${CMAKE_CURRENT_DIR})

SET(CMAKE_RUNTIME_OUTPUT_DIRECTORY $<$<CONFIG:Debug>:${CMAKE_SOURCE_DIR}/bin>)

SET_PROPERTY(DIRECTORY APPEND PROPERTY COMPILE_DEFINITIONS GLEW_STATIC)